import React from 'react';
import SearchBar from '../SearchBar';

function Header() {
  return (
    <div className="Header">
        <h1>Super Shop</h1>
        <SearchBar />
    </div>
  );
}

export default Header;