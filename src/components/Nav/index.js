import React from 'react';
import SearchBar from '../SearchBar';

function Header() {
  return (
    <div className="Header">
        <p>Super Shop</p>
        <SearchBar />
    </div>
  );
}

export default Header;